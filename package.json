{
  "name": "testrail-run-local-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "node --check src/*.js public/*.js tests/*.js"
  }
}
